function setup() {
  createCanvas(windowWidth, windowHeight);
  colorMode(RGB);
}

function draw() {
  background(124, 252, 0);

  //circle
  strokeWeight(2);
  circle(100, 100, 100)

  //square
  square(175, 50, 100);
}
