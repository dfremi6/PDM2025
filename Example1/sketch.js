function setup() {
  createCanvas(windowHeight, windowHeight);
  colorMode(RGB);
}

function draw() {
  background(124, 252, 0);

  //circle
  strokeWeight(2);
  circle(100, 100, 100)

  //square
  square(175, 50, 100);
}
